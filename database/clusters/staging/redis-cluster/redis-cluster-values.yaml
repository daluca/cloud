---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: redis-cluster
  namespace: database
spec:
  chart:
    spec:
      version: ">=0.14.0 <0.15.0"
  test:
    enable: false
  values:
    redisCluster:
      persistenceEnabled: false
