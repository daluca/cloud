---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns
  namespace: kube-system
spec:
  test:
    enable: false
  values:
    domainFilters:
      - ${PRIMARY_DOMAIN}
