---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns
  namespace: kube-system
spec:
  chart:
    spec:
      version: ">=1.0.0 <2.0.0"
  test:
    enable: false
  values:
    domainFilters:
      - ${TERTIARY_DOMAIN}
