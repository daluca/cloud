---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: gatekeeper-policy-manager
  namespace: gatekeeper-system
spec:
  chart:
    spec:
      version: ">=0.4.0 <0.5.0"
  test:
    enable: false
  values:
    replicaCount: 1
