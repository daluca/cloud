---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metrics-server
  namespace: monitoring
spec:
  chart:
    spec:
      version: ">=3.0.0 <4.0.0"
  test:
    enable: false
  values:
    replicas: 2
