---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: keycloak-operator
  namespace: identity
spec:
  chart:
    spec:
      version: ">=0.0.0 <0.1.0"
  test:
    enable: false
