---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: openldap
  namespace: identity
spec:
  test:
    enable: false
  values:
    replicaCount: 2
    persistence:
      size: 1Gi
